> [!NOTE]
> `base/` is unique compared to other module directories.
> Rather than housing separate independent modules, it contains a single monolithic `base` module
> that enables all the things you'd turn on regardless. Less modular, but keeps imports clean.
