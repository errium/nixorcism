{
  services.displayManager.ly = {
    enable = true;

    settings = {
      bigclock = "en";
      allow_empty_password = true;
      animation = "colormix";
      auth_fails = 5;
      clear_password = true;
    };
  };
}
